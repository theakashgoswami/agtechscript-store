# AG TechScript Store — Frontend Env
# Copy to .env.local

# Worker URL (set after deploying backend)
# Leave empty for local dev (uses DummyJSON as fallback)
VITE_API_URL=https://ag-techscript-store.YOUR_ACCOUNT.workers.dev
# OR custom domain:
# VITE_API_URL=https://products-api.agtechscript.in
